# Limpieza del repositorio

Consultar para limpieza del repositorio. Rutas relativas a la raiz; contrastar valores con el codigo.

## Limpieza Realizada

Se eliminaron archivos y carpetas que no estaban en uso:

- README viejo de plantilla Vite.
- `src/components/`.
- `src/assets/` de plantilla.
- `public/favicon.svg`.
- `public/icons.svg`.
- `image/AGENT_HANDOFF/`.
- `output/playwright/`.
- `.playwright-cli/`.
- `.playwright-mcp/`.

No recrear estos elementos salvo que vuelvan a tener un uso real.
